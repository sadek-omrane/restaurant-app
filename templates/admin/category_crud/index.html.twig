{% extends 'admin/index.html.twig' %}
{% block title %}Category index
{% endblock %}

{% block adminContent %}
	<div style="min-height: 70vh;">
		<h1 style="margin: 50px 0 40px 0;text-align: center;">Category index</h1>
		<div style="display:flex; flex-direction: column;align-items: flex-start;">
			{% for message in app.flashes('success') %}
				<p class="alert alert-success">
					{{ message }}
				</p>
			{% endfor %}
			{% for message in app.flashes('danger') %}
				<p class="alert alert-danger">
					{{ message }}
				</p>
			{% endfor %}
		</div>
		<table class="table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Name</th>
					<th>actions</th>
				</tr>
			</thead>
			<tbody>
				{% for category in categories %}
					<tr>
						<td>{{ category.id }}</td>
						<td>{{ category.name }}</td>
						<td>
							<a class="btn btn-primary" href="{{ path('app_admin_category_crud_show', {'id': category.id}) }}">show</a>
							<a class="btn btn-primary" href="{{ path('app_admin_category_crud_edit', {'id': category.id}) }}">edit</a>
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="3">no records found</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		<a class="btn btn-primary" style="margin: 0 0 50px 0;" href="{{ path('app_admin_category_crud_new') }}">Create new</a>
	</div>
{% endblock %}
