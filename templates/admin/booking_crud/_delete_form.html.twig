<form method="post" action="{{ path('app_admin_booking_crud_delete', {'id': booking.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking.id) }}">
	<button class="btn btn-primary">Delete</button>
</form>
